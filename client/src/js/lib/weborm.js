var _createService = function (config) {
        var service = {a: 12};

        return service;
    },
    /**
     * 获取所需的url
     * @private
     */
    _getRequireUrl = function () {

    },
    /**
     * restful接口获取字段
     * @param url
     * @param property [Map] 字段名称
     * @private
     */
    _getData = function (url, property) {

    };

module.exports = {
    createService: _createService
};